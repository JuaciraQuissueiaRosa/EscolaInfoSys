@model EscolaInfoSys.Models.ViewModels.ProfileViewModel


@{
    ViewData["Title"] = "My Profile";
}

<div class="page-body">
    <div class="container-xl">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card card-md">
                    <div class="card-body text-center">
                        <h3 class="card-title mb-4"><i class="fa fa-user"></i> My Profile</h3>

                        @if (!string.IsNullOrEmpty(Model.ProfilePhoto))
                        {
                            <img src="~/uploads/@Model.ProfilePhoto" class="rounded-circle mb-3" style="width: 120px;" alt="Profile Photo" />
                        }
                        else
                        {
                            <div class="mb-3">
                                <span class="avatar avatar-xl" style="background-image: url('/img/avatar-placeholder.png')"></span>
                            </div>
                        }

                        <p class="mb-1"><i class="fa fa-user me-2"></i><strong>Full Name:</strong> @Model.FullName</p>
                        <p class="mb-1"><i class="fa fa-envelope me-2"></i><strong>Email:</strong> @Model.Email</p>
                        <p class="mb-0"><i class="fa fa-user-tag me-2"></i><strong>Role:</strong> @Model.Role</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}


